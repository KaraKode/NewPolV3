import { PolarisItemBase } from "./base-item.mjs";

/**
 * Équipement générique : tout ce qui se transporte sans règle propre.
 * Le socle commun (quantité, encombrement, prix) suffit.
 */
export class PolarisEquipement extends PolarisItemBase {}
